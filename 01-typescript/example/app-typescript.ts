const d1: number = Number(prompt('Введите первое число'));
const d2: number = Number(prompt('Введите второе число'));

function summ(a: number, b: number): number {
    return a + b;
}

console.log(summ(d1, d2));
